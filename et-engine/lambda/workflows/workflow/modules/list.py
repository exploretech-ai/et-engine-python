import json

def handler(event, context):

    # connect to the db

    # query graph

    # extract nodes from graph text

    # reformat as text

    return {
            'statusCode': 200,
            'body': json.dumps("this is a list of modules")
        }
